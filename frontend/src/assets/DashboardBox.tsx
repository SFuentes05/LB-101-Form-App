import { Box, BoxProps } from "@mui/material";
import { styled } from "@mui/system";

const DashboardBox = styled(Box)<BoxProps>({
  backgroundColor: "#ffffff",
  borderRadius: "20px",
  border: "1px solid #d6d6d6",
  overflow: "hidden",
});

export default DashboardBox;
